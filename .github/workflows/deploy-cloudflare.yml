name: Deploy to Cloudflare Pages

on:
  repository_dispatch:
    types: [deploy-wordpress-content]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "✅ Workflow triggered! Event=${{ github.event_name }}"

  // ========================================================================
  // LINKTREE MODE - Full customization interface
  // ========================================================================
  return (
    <div className="link-page-fluid-root space-y-6 px-2 sm:px-4" style={{ maxWidth: 'var(--content-max-width, 100%)', margin: '0 auto' }}>

      {/* ====================================================================
          PREMIUM RESPONSIVE LAYOUT - CSS Grid with template areas
          Desktop/Laptop (1100px+)

          Architecture:
          - Main grid with 5 columns: 4 for settings + 1 for preview
          - Settings sections flow and expand to fill space
          - Preview spans multiple rows on the right
          - Button Links is a row section, not a blocking column
          - Save Bar spans full width at bottom
          ==================================================================== */}
      <div
        className="hidden min-[1100px]:grid gap-1.5 p-2 rounded-2xl border border-white/10 bg-white/[0.02] mx-auto"
        style={{
          /* 5 columns: 4 flexible + preview column */
          gridTemplateColumns: 'repeat(4, minmax(120px, 1fr)) minmax(200px, 240px)',
          /* Rows: all auto to fit content naturally */
          gridTemplateRows: 'auto auto auto auto',
          gridTemplateAreas: `
            "settings settings settings settings preview"
            "social   social   social   social   preview"
            "buttons  buttons  buttons  buttons  preview"
            "save     save     save     save     save"
          `,
          height: 'calc(100vh - 100px)',
          minHeight: '420px',
          maxWidth: '1400px',
        }}
      >
        {/* AREA: Settings - Top section with profile/style settings */}
        <div
          className="grid gap-1.5 p-2 rounded-xl"
          style={{
            gridArea: 'settings',
            gridTemplateColumns: 'repeat(auto-fit, minmax(140px, 1fr))',
            gridAutoRows: 'auto',
            alignContent: 'start',
            backgroundColor: 'rgba(0,0,0,0.2)',
            border: '1px solid rgba(255,255,255,0.1)',
          }}
        >
          {/* Row 1: Photo | Name | Bio */}
          <div
            className="rounded-xl flex flex-col p-2"
            style={{
              backgroundColor: 'rgba(239, 68, 68, 0.3)',
              border: '2px solid rgba(239, 68, 68, 0.6)',
            }}
          >
            <span className="text-[9px] font-bold text-red-300 uppercase mb-1">Photo</span>
            <div className="flex items-center gap-3">
              <div className="flex flex-col items-center">
                <div
                  className="w-12 h-12 rounded-full bg-black/40 border border-white/20 flex items-center justify-center overflow-hidden flex-shrink-0"
                  style={{ backgroundImage: pageData?.profile_image_url ? `url(${pageData.profile_image_url})` : undefined, backgroundSize: 'cover', backgroundPosition: 'center' }}
                >
                  {!pageData?.profile_image_url && <span className="text-white/40 text-lg">👤</span>}
                </div>
                {/* B&W / Color toggle */}
                <div className="mt-1 flex gap-1">
                  <button
                    type="button"
                    onClick={() => { setLinksSettings(prev => ({ ...prev, showColorPhoto: false })); setHasUnsavedChanges(true); }}
                    className={`px-1.5 py-0.5 rounded text-[8px] border transition-colors ${
                      !linksSettings.showColorPhoto
                        ? 'bg-red-500/30 border-red-400 text-red-200'
                        : 'bg-black/20 border-white/20 text-white/60'
                    }`}
                  >
                    B&W
                  </button>
                  <button
                    type="button"
                    onClick={() => { setLinksSettings(prev => ({ ...prev, showColorPhoto: true })); setHasUnsavedChanges(true); }}
                    className={`px-1.5 py-0.5 rounded text-[8px] border transition-colors ${
                      linksSettings.showColorPhoto
                        ? 'bg-red-500/30 border-red-400 text-red-200'
                        : 'bg-black/20 border-white/20 text-white/60'
                    }`}
                  >
                    Color
                  </button>
                </div>
              </div>
              <input
                type="file"
                accept="image/jpeg,image/png,image/webp"
                onChange={handleImageUpload}
                className="hidden"
                id="grid-photo-upload"
              />
              <label
                htmlFor="grid-photo-upload"
                className="px-2 py-1 rounded text-[10px] bg-white/10 border border-white/20 text-white/80 hover:bg-white/20 cursor-pointer"
              >
                {pageData?.profile_image_url ? 'Change' : 'Upload'}
              </label>
            </div>
          </div>

          <div
            className="rounded-xl flex flex-col p-2"
            style={{
              backgroundColor: 'rgba(249, 115, 22, 0.3)',
              border: '2px solid rgba(249, 115, 22, 0.6)',
            }}
          >
            <span className="text-[9px] font-bold text-orange-300 uppercase mb-1">Display Name</span>
            <div className="grid grid-cols-2 gap-1.5">
              <input
                type="text"
                value={formData.display_first_name}
                onChange={(e) => handleInputChange('display_first_name', e.target.value)}
                placeholder="First"
                className="w-full px-2 py-1 rounded bg-black/40 border border-white/20 text-white text-[10px] focus:border-orange-400 focus:outline-none"
              />
              <input
                type="text"
                value={formData.display_last_name}
                onChange={(e) => handleInputChange('display_last_name', e.target.value)}
                placeholder="Last"
                className="w-full px-2 py-1 rounded bg-black/40 border border-white/20 text-white text-[10px] focus:border-orange-400 focus:outline-none"
              />
            </div>
          </div>

          <div
            className="rounded-xl flex flex-col p-2"
            style={{
              backgroundColor: 'rgba(234, 179, 8, 0.3)',
              border: '2px solid rgba(234, 179, 8, 0.6)',
            }}
          >
            <span className="text-[9px] font-bold text-yellow-300 uppercase mb-1">Bio <span className="font-normal text-yellow-300/60">({linksSettings.bio.length}/80)</span></span>
            <textarea
              value={linksSettings.bio}
              onChange={(e) => {
                if (e.target.value.length <= 80) {
                  setLinksSettings(prev => ({ ...prev, bio: e.target.value }));
                  setHasUnsavedChanges(true);
                }
              }}
              placeholder="Short bio..."
              rows={1}
              className="w-full px-2 py-1 rounded bg-black/40 border border-white/20 text-white text-[10px] focus:border-yellow-400 focus:outline-none resize-none"
            />
          </div>

          {/* Row 2: Color | Style | Email */}
          <div
            className="rounded-xl flex flex-col p-2"
            style={{
              backgroundColor: 'rgba(34, 197, 94, 0.3)',
              border: '2px solid rgba(34, 197, 94, 0.6)',
            }}
          >
            <span className="text-[9px] font-bold text-green-300 uppercase mb-1">Accent Color</span>
            <HexColorPicker
              color={linksSettings.accentColor}
              onChange={(color) => {
                setLinksSettings(prev => ({ ...prev, accentColor: color }));
                setHasUnsavedChanges(true);
              }}
              style={{ width: '100%', height: '60px' }}
            />
            <div className="flex items-center gap-1.5 mt-1">
              <div className="w-4 h-4 rounded border border-white/20" style={{ backgroundColor: linksSettings.accentColor }} />
              <HexColorInput
                color={linksSettings.accentColor}
                onChange={(color) => {
                  setLinksSettings(prev => ({ ...prev, accentColor: color }));
                  setHasUnsavedChanges(true);
                }}
                prefixed
                className="flex-1 px-2 py-1 rounded bg-black/40 border border-white/20 text-white text-[10px] font-mono uppercase focus:outline-none"
              />
            </div>
          </div>

          <div
            className="rounded-xl flex flex-col p-2"
            style={{
              backgroundColor: 'rgba(6, 182, 212, 0.3)',
              border: '2px solid rgba(6, 182, 212, 0.6)',
            }}
          >
            <span className="text-[9px] font-bold text-cyan-300 uppercase mb-1">Style Options</span>
            <div className="space-y-1">
              <div className="flex gap-1">
                <button
                  onClick={() => { setLinksSettings(prev => ({ ...prev, iconStyle: 'light' })); setHasUnsavedChanges(true); }}
                  className={`flex-1 px-1.5 py-0.5 rounded text-[8px] border ${linksSettings.iconStyle === 'light' ? 'bg-cyan-500/30 border-cyan-400 text-cyan-200' : 'bg-black/20 border-white/20 text-white/60'}`}
                >Light</button>
                <button
                  onClick={() => { setLinksSettings(prev => ({ ...prev, iconStyle: 'dark' })); setHasUnsavedChanges(true); }}
                  className={`flex-1 px-1.5 py-0.5 rounded text-[8px] border ${linksSettings.iconStyle === 'dark' ? 'bg-cyan-500/30 border-cyan-400 text-cyan-200' : 'bg-black/20 border-white/20 text-white/60'}`}
                >Dark</button>
              </div>
              <div className="flex gap-1">
                <button
                  onClick={() => { setLinksSettings(prev => ({ ...prev, font: 'synonym' })); setHasUnsavedChanges(true); }}
                  className={`flex-1 px-1.5 py-0.5 rounded text-[8px] border ${linksSettings.font === 'synonym' ? 'bg-cyan-500/30 border-cyan-400 text-cyan-200' : 'bg-black/20 border-white/20 text-white/60'}`}
                >Synonym</button>
                <button
                  onClick={() => { setLinksSettings(prev => ({ ...prev, font: 'taskor' })); setHasUnsavedChanges(true); }}
                  className={`flex-1 px-1.5 py-0.5 rounded text-[8px] border ${linksSettings.font === 'taskor' ? 'bg-cyan-500/30 border-cyan-400 text-cyan-200' : 'bg-black/20 border-white/20 text-white/60'}`}
                >Taskor</button>
              </div>
              <div className="flex gap-1">
                <button
                  onClick={() => { setLinksSettings(prev => ({ ...prev, nameWeight: 'bold' })); setHasUnsavedChanges(true); }}
                  className={`flex-1 px-1.5 py-0.5 rounded text-[8px] border ${linksSettings.nameWeight === 'bold' ? 'bg-cyan-500/30 border-cyan-400 text-cyan-200' : 'bg-black/20 border-white/20 text-white/60'}`}
                >Bold</button>
                <button
                  onClick={() => { setLinksSettings(prev => ({ ...prev, nameWeight: 'normal' })); setHasUnsavedChanges(true); }}
                  className={`flex-1 px-1.5 py-0.5 rounded text-[8px] border ${linksSettings.nameWeight === 'normal' ? 'bg-cyan-500/30 border-cyan-400 text-cyan-200' : 'bg-black/20 border-white/20 text-white/60'}`}
                >Regular</button>
              </div>
            </div>
          </div>

          <div
            className="rounded-xl flex flex-col p-2"
            style={{
              backgroundColor: 'rgba(168, 85, 247, 0.3)',
              border: '2px solid rgba(168, 85, 247, 0.6)',
            }}
          >
            <span className="text-[9px] font-bold text-purple-300 uppercase mb-1">Email</span>
            <input
              type="email"
              value={formData.email}
              onChange={(e) => handleInputChange('email', e.target.value)}
              placeholder="you@email.com"
              className="w-full px-2 py-1 rounded bg-black/40 border border-white/20 text-white text-[10px] focus:border-purple-400 focus:outline-none"
            />
            <span className="text-[8px] text-purple-200/50">Shown on page</span>
          </div>

          {/* Row 3: Phone | Social (spans 2) */}
          <div
            className="rounded-xl flex flex-col p-2"
            style={{
              backgroundColor: 'rgba(236, 72, 153, 0.3)',
              border: '2px solid rgba(236, 72, 153, 0.6)',
            }}
          >
            <span className="text-[9px] font-bold text-pink-300 uppercase mb-1">Phone</span>
            <input
              type="tel"
              value={formData.phone}
              onChange={(e) => handleInputChange('phone', e.target.value)}
              placeholder="(555) 123-4567"
              className="w-full px-2 py-1 rounded bg-black/40 border border-white/20 text-white text-[10px] focus:border-pink-400 focus:outline-none"
            />
            <div className="flex gap-2 mt-1">
              <label className="flex items-center gap-0.5 cursor-pointer">
                <input
                  type="checkbox"
                  checked={formData.show_call_button}
                  onChange={(e) => handleInputChange('show_call_button', e.target.checked)}
                  className="w-2.5 h-2.5 rounded"
                />
                <span className="text-[8px] text-pink-200/70">Call</span>
              </label>
              <label className="flex items-center gap-0.5 cursor-pointer">
                <input
                  type="checkbox"
                  checked={formData.show_text_button}
                  onChange={(e) => handleInputChange('show_text_button', e.target.checked)}
                  className="w-2.5 h-2.5 rounded"
                />
                <span className="text-[8px] text-pink-200/70">Text</span>
              </label>
            </div>
          </div>

        </div>

        {/* AREA: Social Links - With disable logic and 2 custom slots */}
        <div
          className="rounded-xl flex flex-col p-2"
          style={{
            gridArea: 'social',
            backgroundColor: 'rgba(99, 102, 241, 0.3)',
            border: '2px solid rgba(99, 102, 241, 0.6)',
          }}
        >
          <span className="text-[9px] font-bold text-indigo-300 uppercase mb-2">Social Links <span className="font-normal text-indigo-300/60">({filledSocialLinks}/6 shown)</span></span>
          <div className="grid grid-cols-4 gap-1.5">
            {/* Built-in social links with disable logic */}
            <div className="flex flex-col">
              <label className="text-[8px] text-indigo-200/60 uppercase mb-0.5">Facebook</label>
              <input type="url" value={formData.facebook_url} onChange={(e) => handleInputChange('facebook_url', e.target.value)} disabled={filledSocialLinks >= 6 && !formData.facebook_url} placeholder="facebook.com/..." className="w-full px-2 py-1 rounded bg-black/40 border border-white/20 text-white text-[10px] focus:border-indigo-400 focus:outline-none disabled:opacity-40" />
            </div>
            <div className="flex flex-col">
              <label className="text-[8px] text-indigo-200/60 uppercase mb-0.5">Instagram</label>
              <input type="url" value={formData.instagram_url} onChange={(e) => handleInputChange('instagram_url', e.target.value)} disabled={filledSocialLinks >= 6 && !formData.instagram_url} placeholder="instagram.com/..." className="w-full px-2 py-1 rounded bg-black/40 border border-white/20 text-white text-[10px] focus:border-indigo-400 focus:outline-none disabled:opacity-40" />
            </div>
            <div className="flex flex-col">
              <label className="text-[8px] text-indigo-200/60 uppercase mb-0.5">X/Twitter</label>
              <input type="url" value={formData.twitter_url} onChange={(e) => handleInputChange('twitter_url', e.target.value)} disabled={filledSocialLinks >= 6 && !formData.twitter_url} placeholder="x.com/..." className="w-full px-2 py-1 rounded bg-black/40 border border-white/20 text-white text-[10px] focus:border-indigo-400 focus:outline-none disabled:opacity-40" />
            </div>
            <div className="flex flex-col">
              <label className="text-[8px] text-indigo-200/60 uppercase mb-0.5">YouTube</label>
              <input type="url" value={formData.youtube_url} onChange={(e) => handleInputChange('youtube_url', e.target.value)} disabled={filledSocialLinks >= 6 && !formData.youtube_url} placeholder="youtube.com/..." className="w-full px-2 py-1 rounded bg-black/40 border border-white/20 text-white text-[10px] focus:border-indigo-400 focus:outline-none disabled:opacity-40" />
            </div>
            <div className="flex flex-col">
              <label className="text-[8px] text-indigo-200/60 uppercase mb-0.5">TikTok</label>
              <input type="url" value={formData.tiktok_url} onChange={(e) => handleInputChange('tiktok_url', e.target.value)} disabled={filledSocialLinks >= 6 && !formData.tiktok_url} placeholder="tiktok.com/..." className="w-full px-2 py-1 rounded bg-black/40 border border-white/20 text-white text-[10px] focus:border-indigo-400 focus:outline-none disabled:opacity-40" />
            </div>
            <div className="flex flex-col">
              <label className="text-[8px] text-indigo-200/60 uppercase mb-0.5">LinkedIn</label>
              <input type="url" value={formData.linkedin_url} onChange={(e) => handleInputChange('linkedin_url', e.target.value)} disabled={filledSocialLinks >= 6 && !formData.linkedin_url} placeholder="linkedin.com/..." className="w-full px-2 py-1 rounded bg-black/40 border border-white/20 text-white text-[10px] focus:border-indigo-400 focus:outline-none disabled:opacity-40" />
            </div>
            {/* 2 Custom social link slots with icon pickers */}
            {[0, 1].map((slotIndex) => {
              const customLink = customSocialLinks[slotIndex];
              const isDisabled = filledSocialLinks >= 6 && !customLink?.url;
              return (
                <div key={slotIndex} className="flex flex-col">
                  <label className="text-[8px] text-indigo-200/60 uppercase mb-0.5">Custom {slotIndex + 1}</label>
                  <div className="relative flex">
                    <button
                      type="button"
                      onClick={() => !isDisabled && setShowSocialIconPicker(showSocialIconPicker === slotIndex ? null : slotIndex)}
                      disabled={isDisabled}
                      className={`flex items-center justify-center w-7 rounded-l bg-black/40 border border-r-0 border-white/20 transition-colors ${isDisabled ? 'opacity-40' : 'hover:border-indigo-400'}`}
                    >
                      {customLink?.icon ? (
                        <svg className="w-3 h-3 text-indigo-300" fill="none" stroke="currentColor" strokeWidth="2" viewBox="0 0 24 24">
                          <path d={LINK_ICONS.find(i => i.name === customLink.icon)?.path || ''} />
                        </svg>
                      ) : (
                        <svg className="w-3 h-3 text-white/30" fill="none" stroke="currentColor" strokeWidth="2" viewBox="0 0 24 24">
                          <path strokeLinecap="round" strokeLinejoin="round" d="M12 4v16m8-8H4" />
                        </svg>
                      )}
                    </button>
                    <input
                      type="url"
                      value={customLink?.url || ''}
                      onChange={(e) => {
                        const newUrl = e.target.value;
                        setCustomSocialLinks(prev => {
                          const updated: typeof prev = [];
                          for (let i = 0; i < 2; i++) {
                            if (i === slotIndex) {
                              updated[i] = { id: prev[i]?.id || `social-${i}`, url: newUrl, icon: prev[i]?.icon || 'Globe' };
                            } else if (prev[i]) {
                              updated[i] = prev[i];
                            }
                          }
                          return updated;
                        });
                        setHasUnsavedChanges(true);
                      }}
                      disabled={isDisabled}
                      placeholder={customLink?.icon ? `${LINK_ICONS.find(i => i.name === customLink.icon)?.label || 'Custom'}` : 'URL'}
                      className="flex-1 min-w-0 px-2 py-1 rounded-r bg-black/40 border border-white/20 text-white text-[10px] focus:border-indigo-400 focus:outline-none disabled:opacity-40"
                    />
                    {/* Icon Picker Dropdown */}
                    {showSocialIconPicker === slotIndex && (
                      <div className="absolute z-20 top-full mt-1 left-0 w-36 max-h-24 overflow-y-auto rounded bg-[#1a1a1a] border border-white/20 shadow-xl">
                        <div className="grid grid-cols-6 gap-0.5 p-1">
                          {LINK_ICONS.map(icon => (
                            <button
                              key={icon.name}
                              type="button"
                              onClick={() => {
                                setCustomSocialLinks(prev => {
                                  const updated: typeof prev = [];
                                  for (let i = 0; i < 2; i++) {
                                    if (i === slotIndex) {
                                      updated[i] = { id: prev[i]?.id || `social-${i}`, url: prev[i]?.url || '', icon: icon.name };
                                    } else if (prev[i]) {
                                      updated[i] = prev[i];
                                    }
                                  }
                                  return updated;
                                });
                                setShowSocialIconPicker(null);
                                setHasUnsavedChanges(true);
                              }}
                              className={`p-1 rounded transition-colors ${customLink?.icon === icon.name ? 'bg-indigo-500/20 text-indigo-300' : 'hover:bg-white/10 text-white/70'}`}
                              title={icon.label}
                            >
                              <svg className="w-2.5 h-2.5" fill="none" stroke="currentColor" strokeWidth="2" viewBox="0 0 24 24">
                                <path d={icon.path} />
                              </svg>
                            </button>
                          ))}
                        </div>
                      </div>
                    )}
                  </div>
                </div>
              );
            })}
          </div>
        </div>

        {/* AREA: Button Links - Full functionality with reorder, icons, edit/delete */}
        <div
          className="rounded-xl flex flex-col p-2"
          style={{
            gridArea: 'buttons',
            backgroundColor: 'rgba(251, 146, 60, 0.3)',
            border: '2px solid rgba(251, 146, 60, 0.6)',
          }}
        >
          <span className="text-[9px] font-bold text-orange-300 uppercase mb-1.5">Button Links ({2 + customLinks.length})</span>

          {/* Links list with reordering */}
          <div className="flex flex-wrap gap-1.5 mb-2">
            {(() => {
              const linkOrder = linksSettings.linkOrder || ['join-team', 'learn-about'];
              const customLinkMap = new Map(customLinks.map(l => [l.id, l]));
              const allLinkIds = [...linkOrder];
              customLinks.forEach(link => { if (!allLinkIds.includes(link.id)) allLinkIds.push(link.id); });
              if (!allLinkIds.includes('join-team')) allLinkIds.unshift('join-team');
              if (!allLinkIds.includes('learn-about')) { const idx = allLinkIds.indexOf('join-team'); allLinkIds.splice(idx + 1, 0, 'learn-about'); }

              return allLinkIds.map((linkId, index) => {
                const isDefault = linkId === 'join-team' || linkId === 'learn-about';
                const customLink = customLinkMap.get(linkId);
                if (!isDefault && !customLink) return null;

                const label = isDefault ? (linkId === 'join-team' ? 'Join Team' : 'About eXp') : customLink?.label;
                const isEditing = editingLinkId === linkId;

                return (
                  <div
                    key={linkId}
                    className={`flex items-center gap-1 px-1.5 py-1 rounded text-[9px] ${
                      isEditing ? 'bg-orange-500/30 border border-orange-400' :
                      isDefault ? 'bg-green-500/20 border border-green-500/40' : 'bg-white/10 border border-white/20'
                    }`}
                  >
                    {/* Reorder buttons */}
                    <div className="flex flex-col -my-0.5">
                      <button type="button" onClick={() => { if (index > 0) { const newOrder = [...allLinkIds]; [newOrder[index-1], newOrder[index]] = [newOrder[index], newOrder[index-1]]; setLinksSettings(prev => ({...prev, linkOrder: newOrder})); setHasUnsavedChanges(true); }}} disabled={index === 0} className="p-0 hover:text-orange-300 disabled:opacity-20">
                        <svg className="w-2 h-2" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path strokeWidth={2} d="M5 15l7-7 7 7" /></svg>
                      </button>
                      <button type="button" onClick={() => { if (index < allLinkIds.length - 1) { const newOrder = [...allLinkIds]; [newOrder[index], newOrder[index+1]] = [newOrder[index+1], newOrder[index]]; setLinksSettings(prev => ({...prev, linkOrder: newOrder})); setHasUnsavedChanges(true); }}} disabled={index === allLinkIds.length - 1} className="p-0 hover:text-orange-300 disabled:opacity-20">
                        <svg className="w-2 h-2" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path strokeWidth={2} d="M19 9l-7 7-7-7" /></svg>
                      </button>
                    </div>
                    {/* Icon */}
                    {customLink?.icon && (
                      <svg className="w-2.5 h-2.5 text-orange-300" fill="none" stroke="currentColor" strokeWidth="2" viewBox="0 0 24 24">
                        <path d={LINK_ICONS.find(i => i.name === customLink.icon)?.path || ''} />
                      </svg>
                    )}
                    {/* Label */}
                    <span className={`truncate max-w-[60px] ${isDefault ? 'text-green-300' : 'text-white/80'}`}>{label}</span>
                    {isDefault && <span className="text-[7px] opacity-50">def</span>}
                    {/* Edit/Delete for custom links */}
                    {!isDefault && (
                      <>
                        <button type="button" onClick={() => { setNewLinkLabel(customLink?.label || ''); setNewLinkUrl(customLink?.url || ''); setNewLinkIcon(customLink?.icon || null); setEditingLinkId(linkId); }} className="text-orange-300/60 hover:text-orange-300">
                          <svg className="w-2.5 h-2.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path strokeWidth={2} d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z" /></svg>
                        </button>
                        <button type="button" onClick={() => { setCustomLinks(prev => prev.filter(l => l.id !== linkId)); setLinksSettings(prev => ({...prev, linkOrder: prev.linkOrder.filter(id => id !== linkId)})); setHasUnsavedChanges(true); if (editingLinkId === linkId) { setEditingLinkId(null); setNewLinkLabel(''); setNewLinkUrl(''); setNewLinkIcon(null); }}} className="text-red-400/60 hover:text-red-400">
                          <svg className="w-2.5 h-2.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path strokeWidth={2} d="M6 18L18 6M6 6l12 12" /></svg>
                        </button>
                      </>
                    )}
                  </div>
                    activeTab === 'links'
                      ? 'text-[#22c55e] bg-[#22c55e]/10'
                      : 'text-[#e5e4dd]/60 hover:text-[#e5e4dd]'
                  }`}
                >
          {/* Add/Edit form */}
          <div className="flex items-center gap-1.5 flex-wrap">
            <input type="text" value={newLinkLabel} onChange={(e) => setNewLinkLabel(e.target.value)} placeholder="Label" className="flex-1 min-w-[60px] max-w-[100px] px-2 py-1 rounded bg-black/40 border border-white/20 text-white text-[9px] focus:border-orange-400 focus:outline-none" />
            <input type="url" value={newLinkUrl} onChange={(e) => setNewLinkUrl(e.target.value)} placeholder="https://..." className="flex-1 min-w-[80px] max-w-[140px] px-2 py-1 rounded bg-black/40 border border-white/20 text-white text-[9px] focus:border-orange-400 focus:outline-none" />
            {/* Icon picker */}
            <div className="relative">
              <button type="button" onClick={() => setShowIconPicker(!showIconPicker)} className="flex items-center gap-0.5 px-1.5 py-1 rounded bg-black/40 border border-white/20 text-[9px] hover:border-orange-400">
                    <span className=\"absolute bottom-0 left-0 right-0 h-0.5 bg-[#22c55e]\" />
                  <svg className="w-3 h-3 text-orange-300" fill="none" stroke="currentColor" strokeWidth="2" viewBox="0 0 24 24"><path d={LINK_ICONS.find(i => i.name === newLinkIcon)?.path || ''} /></svg>
                </button>
                  <span className="text-white/50">Icon</span>
                )}
              </button>
            {/* Mobile tabs show all content here; Desktop shows only Profile+Connect */}
                <div className="absolute z-20 bottom-full mb-1 left-0 w-36 max-h-24 overflow-y-auto rounded bg-[#1a1a1a] border border-white/20 shadow-xl">
                  <div className="grid grid-cols-6 gap-0.5 p-1">
                    <button type="button" onClick={() => { setNewLinkIcon(null); setShowIconPicker(false); }} className={`p-1 rounded ${!newLinkIcon ? 'bg-orange-500/20 text-orange-300' : 'hover:bg-white/10 text-white/70'}`} title="None">
                      <svg className="w-2.5 h-2.5" fill="none" stroke="currentColor" strokeWidth="2" viewBox="0 0 24 24"><path d="M18.364 18.364A9 9 0 005.636 5.636m12.728 12.728A9 9 0 015.636 5.636m12.728 12.728L5.636 5.636" /></svg>
              <div className=\"flex items-center gap-2 px-3 py-2 -mx-2 rounded-lg min-[1024px]:bg-[#22c55e]/10 border-b border-white/10 min-[1024px]:border-b-0\">
                <svg className=\"w-5 h-5 text-[#22c55e]\" fill=\"none\" stroke=\"currentColor\" strokeWidth=\"2\" viewBox=\"0 0 24 24\">
                      <button key={icon.name} type="button" onClick={() => { setNewLinkIcon(icon.name); setShowIconPicker(false); }} className={`p-1 rounded ${newLinkIcon === icon.name ? 'bg-orange-500/20 text-orange-300' : 'hover:bg-white/10 text-white/70'}`} title={icon.label}>
                        <svg className="w-2.5 h-2.5" fill="none" stroke="currentColor" strokeWidth="2" viewBox="0 0 24 24"><path d={icon.path} /></svg>
                <h3 className=\"text-sm font-semibold text-[#22c55e] uppercase tracking-wide\">Profile</h3>
                    ))}
                  {/* Profile Image - Compact */}
                  <div className=\"p-4 rounded-lg bg-black/20 border border-white/10\">
                    <div className=\"flex items-start gap-4\">
                      {/* Profile Image + Style Toggle Column */}
                      <div className=\"flex flex-col items-center flex-shrink-0\">
                        <label
                          htmlFor=\"attraction-profile-image-upload\"
                          className=\"relative w-20 h-20 rounded-full overflow-hidden border-2 border-[#ffd700] bg-black/30 cursor-pointer group\"
                if (newLinkLabel.trim() && newLinkUrl.trim()) {
                          {getProfileImageUrl() ? (
                    setCustomLinks(prev => prev.map(link => link.id === editingLinkId ? { ...link, label: newLinkLabel.trim(), url: newLinkUrl.trim(), icon: newLinkIcon || undefined } : link));
                              src={getProfileImageUrl() || ''}
                              alt=\"Profile\"
                              className=\"w-full h-full object-cover transition-all duration-300\"
                    setCustomLinks(prev => [...prev, { id: newLinkId, label: newLinkLabel.trim(), url: newLinkUrl.trim(), icon: newLinkIcon || undefined, order: customLinks.length }]);
                    setLinksSettings(prev => ({ ...prev, linkOrder: [...(prev.linkOrder || ['join-team', 'learn-about']), newLinkId] }));
                            <div className=\"w-full h-full flex items-center justify-center text-[#ffd700]/40\">
                  setNewLinkLabel(''); setNewLinkUrl(''); setNewLinkIcon(null); setHasUnsavedChanges(true);
                                <path d=\"M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z\"/>
                              </svg>
              disabled={!newLinkLabel.trim() || !newLinkUrl.trim()}
              className={`px-2 py-1 rounded text-[9px] font-medium disabled:opacity-50 ${editingLinkId ? 'bg-orange-500/30 border border-orange-400 text-orange-300' : 'bg-green-500/20 border border-green-500/40 text-green-300'}`}
                          {isUploadingImage && (
                            <div className=\"absolute inset-0 bg-black/60 flex items-center justify-center\">
                              <div className=\"w-6 h-6 border-2 border-[#ffd700] border-t-transparent rounded-full animate-spin\" />
                            </div>
              <button type="button" onClick={() => { setEditingLinkId(null); setNewLinkLabel(''); setNewLinkUrl(''); setNewLinkIcon(null); }} className="px-1.5 py-1 rounded text-[9px] bg-white/10 text-white/60 hover:bg-white/20">Cancel</button>
                          <div className=\"absolute inset-0 bg-black/50 opacity-0 group-hover:opacity-100 transition-opacity flex items-center justify-center\">
                            <svg viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" strokeWidth=\"2\" width=\"20\" height=\"20\" className=\"text-white\">
                              <path d=\"M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4\" />
                              <polyline points=\"17 8 12 3 7 8\" />
                              <line x1=\"12\" y1=\"3\" x2=\"12\" y2=\"15\" />
                            </svg>
          className="rounded-xl flex items-center justify-center gap-3 p-2 flex-wrap"
                        </label>
                        {/* Photo Style Toggle - B&W or Color - Under the image */}
                        <div className=\"mt-2 flex gap-1\">
            border: '2px solid rgba(255, 215, 0, 0.4)',
                            type=\"button\"
                            onClick={() => { setLinksSettings(prev => ({ ...prev, showColorPhoto: false })); setHasUnsavedChanges(true); }}
                            className={`px-2 py-0.5 rounded text-[10px] border transition-colors ${
                              !linksSettings.showColorPhoto
            <div className="w-8 h-8 bg-white rounded flex items-center justify-center">
                                : 'bg-black/20 border-white/10 text-[#e5e4dd]/70 hover:border-white/20'
                            }`}
                          >
              onClick={downloadQRCode}
              className="px-1.5 py-0.5 rounded text-[8px] bg-white/20 text-white/80 hover:bg-white/30"
                          <button
                            type=\"button\"
                            onClick={() => { setLinksSettings(prev => ({ ...prev, showColorPhoto: true })); setHasUnsavedChanges(true); }}
                            className={`px-2 py-0.5 rounded text-[10px] border transition-colors ${
                              linksSettings.showColorPhoto
                                ? 'bg-[#ffd700]/20 border-[#ffd700] text-[#ffd700]'
          <div className={`flex items-center gap-1 px-1.5 py-0.5 rounded text-[8px] ${pageData?.activated ? 'bg-green-500/30 text-green-200' : 'bg-yellow-500/30 text-yellow-200'}`}>
            <div className={`w-1.5 h-1.5 rounded-full ${pageData?.activated ? 'bg-green-400' : 'bg-yellow-400'}`} />
            {pageData?.activated ? 'Live' : 'Draft'}
                            Color
                          </button>
                        </div>
                      </div>
                onClick={() => navigator.clipboard.writeText(`https://saabuildingblocks.com/${pageData.slug}`)}
                className="px-1.5 py-0.5 rounded text-[8px] bg-white/10 text-white/70 hover:bg-white/20"
                        <h4 className=\"text-sm font-medium text-[#ffd700] mb-1\">Profile Image <span className=\"text-red-400\">*</span></h4>
                        <p className=\"text-xs text-[#e5e4dd]/50 mb-2\">Background removed automatically</p>
                        <input
                          ref={attractionFileInputRef}
                href={`https://saabuildingblocks.com/${pageData.slug}`}
                          accept=\"image/jpeg,image/png,image/webp\"
                          onChange={handleImageUpload}
                className="px-1.5 py-0.5 rounded text-[8px] bg-white/10 text-white/70 hover:bg-white/20"
                          className=\"hidden\"
                          id=\"attraction-profile-image-upload\"
                        />
                        <label
                          htmlFor=\"attraction-profile-image-upload\"
                          className=\"inline-flex items-center gap-1.5 px-3 py-1.5 rounded text-xs bg-[#ffd700]/10 border border-[#ffd700]/30 text-[#ffd700] hover:bg-[#ffd700]/20 transition-colors cursor-pointer\"
                        >
                          <svg viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" strokeWidth=\"2\" width=\"14\" height=\"14\">
                            <path d=\"M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4\" />
            className="px-3 py-1.5 rounded-lg bg-[#ffd700] text-[#1a1a1a] text-[10px] font-bold disabled:opacity-50 disabled:cursor-not-allowed hover:bg-[#ffe55c] transition-colors"
                            <line x1=\"12\" y1=\"3\" x2=\"12\" y2=\"15\" />
            {isSaving ? 'Saving...' : 'Save'}
                          {pageData?.profile_image_url ? 'Change' : 'Upload'}
          {!pageData?.activated && (
                        {attractionUploadStatus && (
                          <div className=\"mt-2 text-xs text-blue-400 flex items-center gap-1.5\">
                            <div className=\"w-3 h-3 border-2 border-blue-400 border-t-transparent rounded-full animate-spin\" />
              className="px-3 py-1.5 rounded-lg bg-green-500 text-white text-[10px] font-bold disabled:opacity-50 disabled:cursor-not-allowed hover:bg-green-600 transition-colors"
                          </div>
                        )}
                        {attractionUploadError && (
                          <div className=\"mt-2 text-xs text-red-400\">{attractionUploadError}</div>
                        )}
                      </div>
        {/* AREA: Preview - Spans all content rows on the right */}
                  </div>
          className="rounded-xl flex flex-col items-center p-2"
                  {/* Display Name + Bio Row - Side by side on wide screens only */}
                  <div className=\"grid grid-cols-1 min-[1650px]:grid-cols-2 gap-3\">
            backgroundColor: 'rgba(107, 114, 128, 0.3)',
                    <div className=\"p-4 rounded-lg bg-black/20 border border-white/10\">
                      <h4 className=\"text-sm font-medium text-[#ffd700] mb-3\">Display Name</h4>
                      <div className=\"grid grid-cols-2 gap-3\">
          <span className="text-[9px] font-bold text-gray-300 uppercase tracking-wider mb-2">Live Preview</span>
                          <label className=\"block text-xs text-[#e5e4dd]/60 mb-1\">First Name</label>
                          <input
          <div className="phone-mockup flex-1 w-full max-w-[180px]" style={{ minHeight: '240px' }}>
            <div className="phone-screen h-full flex flex-col items-center p-4 pt-8 overflow-auto">
                            onChange={(e) => handleInputChange('display_first_name', e.target.value)}
                            className=\"w-full px-3 py-2 rounded-lg bg-black/30 border border-white/10 text-[#e5e4dd] text-sm focus:border-[#ffd700]/50 focus:outline-none transition-colors\"
                className="w-20 h-20 rounded-full bg-black/40 border-2 flex items-center justify-center overflow-hidden mb-3"
                        </div>
                  borderColor: linksSettings.accentColor,
                  backgroundImage: pageData?.profile_image_url ? `url(${pageData.profile_image_url})` : undefined,
                  backgroundSize: 'cover',
                  backgroundPosition: 'center',
                            value={formData.display_last_name}
                            onChange={(e) => handleInputChange('display_last_name', e.target.value)}
                            className=\"w-full px-3 py-2 rounded-lg bg-black/30 border border-white/10 text-[#e5e4dd] text-sm focus:border-[#ffd700]/50 focus:outline-none transition-colors\"
                          />
                        </div>
                      </div>
                    </div>
                className="text-sm text-center mb-1"
                    {/* Bio */}
                    <div className=\"p-4 rounded-lg bg-black/20 border border-white/10\">
                      <h4 className=\"text-sm font-medium text-[#ffd700] mb-2\">Bio</h4>
                  fontFamily: linksSettings.font === 'taskor' ? 'var(--font-taskor, sans-serif)' : 'var(--font-synonym, sans-serif)',
                        <textarea
                          value={linksSettings.bio}
                {formData.display_first_name || 'First'} {formData.display_last_name || 'Last'}
                            if (e.target.value.length <= 80) {
                              setLinksSettings(prev => ({ ...prev, bio: e.target.value }));
                              setHasUnsavedChanges(true);
              {linksSettings.bio && (
                <p className="text-[10px] text-white/60 text-center mb-3 px-2">{linksSettings.bio}</p>
                          className=\"w-full px-3 py-2 rounded-lg bg-black/30 border border-white/10 text-[#e5e4dd] text-sm focus:border-[#ffd700]/50 focus:outline-none transition-colors resize-none\"
                          rows={3}
                          placeholder=\"Short description about yourself...\"
              <div className="flex gap-2 mb-3">
                        <span className={`absolute bottom-2 right-2 text-[10px] ${
                  <div className="w-8 h-8 rounded-full flex items-center justify-center text-xs" style={{ backgroundColor: linksSettings.accentColor, color: linksSettings.iconStyle === 'light' ? '#fff' : '#000' }}>📞</div>
                          linksSettings.bio.length >= 60 ? 'text-yellow-400' :
                {formData.show_text_button && formData.phone && (
                  <div className="w-8 h-8 rounded-full flex items-center justify-center text-xs" style={{ backgroundColor: linksSettings.accentColor, color: linksSettings.iconStyle === 'light' ? '#fff' : '#000' }}>💬</div>
                          {linksSettings.bio.length}/80
                {formData.email && (
                  <div className="w-8 h-8 rounded-full flex items-center justify-center text-xs" style={{ backgroundColor: linksSettings.accentColor, color: linksSettings.iconStyle === 'light' ? '#fff' : '#000' }}>✉️</div>
                    </div>
                  </div>
            </div> {/* End PROFILE SECTION */}
              {/* Button Links preview */}
            {/* CONNECT SECTION - Social Links, Phone, Email */}
                <div className="w-full py-2 px-3 rounded-lg text-[9px] text-center" style={{ backgroundColor: linksSettings.accentColor, color: linksSettings.iconStyle === 'light' ? '#fff' : '#000', fontFamily: linksSettings.font === 'taskor' ? 'var(--font-taskor, sans-serif)' : 'var(--font-synonym, sans-serif)' }}>
              {/* Section Header - Full width background band on desktop */}
              <div className=\"flex items-center gap-2 px-3 py-2 -mx-2 rounded-lg min-[1024px]:bg-[#22c55e]/10 border-b border-white/10 min-[1024px]:border-b-0\">
                <div className="w-full py-2 px-3 rounded-lg text-[9px] text-center" style={{ backgroundColor: linksSettings.accentColor, color: linksSettings.iconStyle === 'light' ? '#fff' : '#000', fontFamily: linksSettings.font === 'taskor' ? 'var(--font-taskor, sans-serif)' : 'var(--font-synonym, sans-serif)' }}>
                  <path strokeLinecap=\"round\" strokeLinejoin=\"round\" d=\"M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z\" />
                </div>
                <h3 className=\"text-sm font-semibold text-[#22c55e] uppercase tracking-wide\">Connect</h3>
                  <div key={link.id} className="w-full py-2 px-3 rounded-lg text-[9px] text-center border border-white/20 text-white/60">
                    {link.label || 'Custom Link'}
              {/* Social Links */}
                ))}
              {/* Content wrapper - 2 columns at >1650px */}
              <div className=\"min-[1650px]:grid min-[1650px]:grid-cols-2 min-[1650px]:gap-6 space-y-3 min-[1650px]:space-y-0 mt-3\">
                {/* Column 1: Social Links */}
                <div className=\"space-y-3\">
              <div className=\"p-4 rounded-lg bg-black/20 border border-white/10\">
                <div className=\"flex items-center justify-between mb-3\">
                >
                  Connect
                  {activeTab === 'connect' && (
                    <span className=\"absolute bottom-0 left-0 right-0 h-0.5 bg-[#22c55e]\" />
                  )}
                </button>
                <button
                  onClick={() => setActiveTab('links')}
                  style={{ WebkitTapHighlightColor: 'transparent' } as React.CSSProperties}
                  className={`flex-1 px-2 sm:px-4 py-3 text-xs sm:text-sm font-medium transition-colors relative ${
                    activeTab === 'links'
                      ? 'text-[#22c55e] bg-[#22c55e]/10'
                      : 'text-[#e5e4dd]/60 hover:text-[#e5e4dd]'
                  }`}
                >
                  Links
                  {customLinks.length > 0 && (
                    <span className=\"ml-1 px-1 py-0.5 text-[10px] sm:text-xs rounded-full bg-[#22c55e]/20 text-[#22c55e]\">
                      {customLinks.length}
                    </span>
                  )}
                  {activeTab === 'links' && (
                    <span className=\"absolute bottom-0 left-0 right-0 h-0.5 bg-[#22c55e]\" />
                  )}
                </button>
              </div>
            </div>

            {/* Mobile tabs show all content here; Desktop shows only Profile+Connect */}
            <div className=\"space-y-6\">
            {/* PROFILE SECTION - Photo, Name, Bio */}
            <div className={`space-y-3 ${activeTab === 'profile' ? '' : 'hidden min-[1100px]:block'}`}>
              {/* Section Header - Full width background band on desktop */}
              <div className=\"flex items-center gap-2 px-3 py-2 -mx-2 rounded-lg min-[1100px]:bg-[#22c55e]/10 border-b border-white/10 min-[1100px]:border-b-0\">
                <svg className=\"w-5 h-5 text-[#22c55e]\" fill=\"none\" stroke=\"currentColor\" strokeWidth=\"2\" viewBox=\"0 0 24 24\">
                  <path strokeLinecap=\"round\" strokeLinejoin=\"round\" d=\"M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z\" />
                </svg>
                <h3 className=\"text-sm font-semibold text-[#22c55e] uppercase tracking-wide\">Profile</h3>
              </div>
                  {/* Profile Image - Compact */}
                  <div className=\"p-4 rounded-lg bg-black/20 border border-white/10\">
                    <div className=\"flex items-start gap-4\">
                      {/* Profile Image + Style Toggle Column */}
                      <div className=\"flex flex-col items-center flex-shrink-0\">
                        <label
                          htmlFor=\"attraction-profile-image-upload\"
                          className=\"relative w-20 h-20 rounded-full overflow-hidden border-2 border-[#ffd700] bg-black/30 cursor-pointer group\"
                        >
                          {getProfileImageUrl() ? (
                            <img
                              src={getProfileImageUrl() || ''}
                              alt=\"Profile\"
                              className=\"w-full h-full object-cover transition-all duration-300\"
                            />
                          ) : (
                            <div className=\"w-full h-full flex items-center justify-center text-[#ffd700]/40\">
                              <svg viewBox=\"0 0 24 24\" fill=\"currentColor\" width=\"32\" height=\"32\">
                                <path d=\"M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z\"/>
                              </svg>
                            </div>
                          )}
                          {isUploadingImage && (
                            <div className=\"absolute inset-0 bg-black/60 flex items-center justify-center\">
                              <div className=\"w-6 h-6 border-2 border-[#ffd700] border-t-transparent rounded-full animate-spin\" />
                            </div>
                          )}
                          <div className=\"absolute inset-0 bg-black/50 opacity-0 group-hover:opacity-100 transition-opacity flex items-center justify-center\">
                            <svg viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" strokeWidth=\"2\" width=\"20\" height=\"20\" className=\"text-white\">
                              <path d=\"M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4\" />

                    placeholder=\"Facebook URL\"
                  />
                  <input
                    type=\"url\"
                    value={formData.instagram_url}
                    onChange={(e) => handleInputChange('instagram_url', e.target.value)}
                    disabled={filledSocialLinks >= 6 && !formData.instagram_url}
                    className=\"w-full px-3 py-2 rounded-lg bg-black/30 border border-white/10 text-[#e5e4dd] text-sm focus:border-[#ffd700]/50 focus:outline-none transition-colors disabled:opacity-40\"
                    placeholder=\"Instagram URL\"
                  />
                  <input
                    type=\"url\"
                    value={formData.twitter_url}
                    onChange={(e) => handleInputChange('twitter_url', e.target.value)}
                    disabled={filledSocialLinks >= 6 && !formData.twitter_url}
                    className=\"w-full px-3 py-2 rounded-lg bg-black/30 border border-white/10 text-[#e5e4dd] text-sm focus:border-[#ffd700]/50 focus:outline-none transition-colors disabled:opacity-40\"
                    placeholder=\"X (Twitter) URL\"
                  />
                  <input
                    type=\"url\"
                    value={formData.youtube_url}
                    onChange={(e) => handleInputChange('youtube_url', e.target.value)}
                    disabled={filledSocialLinks >= 6 && !formData.youtube_url}
                    className=\"w-full px-3 py-2 rounded-lg bg-black/30 border border-white/10 text-[#e5e4dd] text-sm focus:border-[#ffd700]/50 focus:outline-none transition-colors disabled:opacity-40\"
                    placeholder=\"YouTube URL\"
                  />
                  <input
                    type=\"url\"
                    value={formData.tiktok_url}
                    onChange={(e) => handleInputChange('tiktok_url', e.target.value)}
                    disabled={filledSocialLinks >= 6 && !formData.tiktok_url}
                    className=\"w-full px-3 py-2 rounded-lg bg-black/30 border border-white/10 text-[#e5e4dd] text-sm focus:border-[#ffd700]/50 focus:outline-none transition-colors disabled:opacity-40\"
                    placeholder=\"TikTok URL\"
                  />
                  <input
                    type=\"url\"
                    value={formData.linkedin_url}
                    onChange={(e) => handleInputChange('linkedin_url', e.target.value)}
                    disabled={filledSocialLinks >= 6 && !formData.linkedin_url}
                    className=\"w-full px-3 py-2 rounded-lg bg-black/30 border border-white/10 text-[#e5e4dd] text-sm focus:border-[#ffd700]/50 focus:outline-none transition-colors disabled:opacity-40\"
                    placeholder=\"LinkedIn URL\"
                  />
                  {/* Custom Social Links (2 slots with icon picker) */}
                  {[0, 1].map((slotIndex) => {
                    const customLink = customSocialLinks[slotIndex];
                    const isDisabled = filledSocialLinks >= 6 && !customLink?.url;
                    return (
                      <div key={slotIndex} className=\"relative flex min-w-0\">
                        <button
                          type=\"button\"
                          onClick={() => !isDisabled && setShowSocialIconPicker(showSocialIconPicker === slotIndex ? null : slotIndex)}
                          disabled={isDisabled}
                          className={`flex items-center justify-center w-10 rounded-l-lg bg-black/40 border border-r-0 border-white/10 transition-colors ${isDisabled ? 'opacity-40' : 'hover:border-[#ffd700]/30'}`}
                        >
                          {customLink?.icon ? (
                            <svg className=\"w-4 h-4 text-[#ffd700]\" fill=\"none\" stroke=\"currentColor\" strokeWidth=\"2\" viewBox=\"0 0 24 24\">
                              <path d={LINK_ICONS.find(i => i.name === customLink.icon)?.path || ''} />
                            </svg>
                          ) : (
                            <svg className=\"w-4 h-4 text-[#e5e4dd]/30\" fill=\"none\" stroke=\"currentColor\" strokeWidth=\"2\" viewBox=\"0 0 24 24\">
                              <path strokeLinecap=\"round\" strokeLinejoin=\"round\" d=\"M12 4v16m8-8H4\" />
                            </svg>
                          )}
                        </button>
                        <input
                          type=\"url

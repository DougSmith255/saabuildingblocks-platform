---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { GenericCard } from '@saa/shared/components/saa/cards/GenericCard';
import { ProfileCyberFrame } from '@saa/shared/components/saa/media/ProfileCyberFrame';

const DOUG_PROFILE_IMAGE = 'https://imagedelivery.net/RZBQ4dWu2c_YEpklnDDxFg/55dbdf32ddc5fbcc-Doug-Profile-Picture.png/public';

const socialLinks = [
  { title: "Instagram", href: "https://instagram.com/dougthemediamaker", icon: "\u{1F4F8}" },
  { title: "YouTube", href: "https://youtube.com/@SmartAgentAlliance", icon: "\u{1F3AC}" },
  { title: "LinkedIn", href: "https://linkedin.com/in/doug-smart", icon: "\u{1F4BC}" },
  { title: "TikTok", href: "https://tiktok.com/@dougthemediamaker", icon: "\u{1F3B5}" },
];

const resourceLinks = [
  { title: "Smart Agent Alliance", description: "Our team website and resources", href: "/", highlight: true },
  { title: "Everything About eXp", description: "Learn about eXp Realty", href: "/about-exp-realty/", highlight: false },
  { title: "Join My eXp Sponsor Team", description: "Partner with the Wolf Pack", href: "/exp-realty-sponsor/", highlight: true },
  { title: "YouTube - For Agents", description: "Training and tips for real estate agents", href: "https://youtube.com/@SmartAgentAlliance", highlight: false },
  { title: "Doug's Tool List", description: "Tech tools I recommend", href: "/sites-and-software/", highlight: false },
];
---

<BaseLayout
  title="Doug Smart - Links"
  description="Quick links and resources from Doug Smart, Co-Founder of Smart Agent Alliance."
  ogTitle="Doug Smart Links | Smart Agent Alliance"
  noHeader
  noFooter
  noScrollIndicator
  noScrollProgress
  noStarBackground
>
  <main id="main-content" class="flex flex-col items-center py-12 px-4 min-h-screen">
    <!-- Profile Section -->
    <div class="text-center mb-8">
      <ProfileCyberFrame client:visible size="md">
        <img
          src={DOUG_PROFILE_IMAGE}
          alt="Doug Smart"
          style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', objectFit: 'cover' }}
        />
      </ProfileCyberFrame>
      <h1 class="text-h4 text-[#e5e4dd] mb-1">Doug Smart</h1>
      <p class="text-[#ffd700] mb-2" style={{ fontSize: 'var(--font-size-caption)' }}>@dougthemediamaker</p>
      <p class="text-[#dcdbd5]/80 max-w-xs mx-auto" style={{ fontSize: 'var(--font-size-caption)' }}>
        Co-Founder, Smart Agent Alliance @ eXp Realty
      </p>
    </div>

    <!-- Social Icons -->
    <div class="flex gap-4 mb-8">
      {socialLinks.map((link) => (
        <a
          href={link.href}
          target="_blank"
          rel="noopener noreferrer"
          class="w-12 h-12 bg-white/10 rounded-full flex items-center justify-center text-xl hover:bg-[#ffd700]/20 transition-colors"
          title={link.title}
        >
          {link.icon}
        </a>
      ))}
    </div>

    <!-- Resource Links -->
    <div class="w-full max-w-md space-y-3">
      {resourceLinks.map((link) => (
        <a
          href={link.href}
          target={link.href.startsWith('http') ? '_blank' : undefined}
          rel={link.href.startsWith('http') ? 'noopener noreferrer' : undefined}
          class="block"
        >
          <GenericCard hover padding="sm" centered>
            <p class="text-body text-[#e5e4dd] font-medium">{link.title}</p>
            {link.description && (
              <p class="text-[#dcdbd5]/80 mt-1" style={{ fontSize: 'var(--font-size-caption)' }}>{link.description}</p>
            )}
          </GenericCard>
        </a>
      ))}
    </div>

    <!-- Email -->
    <div class="mt-8 text-center">
      <a href="mailto:doug@smartagentalliance.com" class="text-link hover:underline">
        doug@smartagentalliance.com
      </a>
      <p class="text-[#dcdbd5]/60 mt-2" style={{ fontSize: 'var(--font-size-caption)' }}>314-320-5606</p>
    </div>

    <!-- Footer -->
    <div class="mt-12 text-center">
      <p class="text-caption text-[#dcdbd5]/40">
        Smart Agent Alliance &bull; eXp Realty
      </p>
    </div>
  </main>
</BaseLayout>
